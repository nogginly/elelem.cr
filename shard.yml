name: elelem
version: 0.1.0
license: MPL2

description: >
  More details soon, though saying the name out loud gives you a clue.

authors:
  - nogginly@icloud.com

targets:
  elelem:
    main: src/elelem_cli.cr

development_dependencies:
  ameba:
    github: crystal-ameba/ameba
  # Records live HTTP once, replays offline forever. Development-only, so the
  # shard's runtime dependency count stays nil.
  wiretap:
    github: nogginly/wiretap.cr
    version: ">= 0.4.1"
